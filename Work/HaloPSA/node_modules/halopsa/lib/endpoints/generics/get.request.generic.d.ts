export declare class haloGetRequest {
    hostname: string;
    accessToken: string;
    endpoint: string;
    constructor(hostname: string, accessToken: string, endpoint: string);
    get<responseType>(params: {
        [key: string]: string | boolean | number;
    }): Promise<responseType>;
}
